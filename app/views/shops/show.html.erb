<div class="col-lg-6 mx-auto">
<div class="row">
  <div class="col-lg-12 mx-auto">
    <%= image_tag @shop.image.variant(resize_to_fill: [500, 500]).processed, class: "d-block mx-auto" %>
  </div>
</div>

<div class="col-lg-12">
  <div class="font-c">
  <p><b>店舗名：<%= @shop.store_name %></b></p>

  <p><b>住所：<%= @shop.address %></b></p>

  <p><b>電話番号：<%= @shop.phone_number %></b></p>

  <p><b>営業時間：<%= @shop.open_hour %></b></p>

  <p><b>一言コメント:<%= @shop.caption %></b></p>
  </div>

  <iframe src=<%= @shop.map_url %> width="510" height="450" frameborder="0" style="border:0;" alloΩΩwfullscreen="" aria-hidden="false" tabindex="0"></iframe>

    <p>
      <%= link_to t(".edit"), edit_shop_path(@shop), method: :get, class: "btn btn-block btn-primary" %>
    </p>
    <p>
      <%= button_tag t(".delete"), class: "btn btn-block btn-warning", data: { toggle: "modal", target: "#delete-shop" } %>
    </p>
  </div>
</div>
</div>

<div class="modal fade" id="delete-shop">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">投稿削除</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p><%= image_tag @shop.image.variant(resize_to_fill: [800, 800]).processed, class: "img-fluid" %></p>
        <p><%= "投稿を削除しますか？" %></p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">キャンセル</button>
        <%= link_to "削除", shop_path(@shop), method: :delete, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
