<div class="row">
  <div class="col-lg-8 mx-auto">
    <div class="logo col-lg-12 mb-3 text-center">
      <h1>Edit Post</h1>
    </div>

    <div class="card">
      <div class="row no-gutters">
        <div class="col-lg-6">
          <% if @shop.image.attached? %>
            <%= image_tag @shop.image.variant(resize_to_fill: [800, 800]).processed, class: "card-img" %>
          <% else %>
            <%= image_tag "no_image", class: "card-img" %>
          <% end %>
        </div>

        <div class="col-lg-6 card-cody">
          <%= render "form", shop: @shop %>
        </div>
      </div>
    </div>
  </div>
</div>